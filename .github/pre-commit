#!/usr/bin/env bash
set -euo pipefail

# Pre-commit hook: Run cargo sqlx prepare in the app directory
cd "$(dirname "$0")"
cd ../../app
echo "[pre-commit] Running: cargo sqlx prepare"
cargo sqlx prepare

cd - > /dev/null

echo "[pre-commit] cargo sqlx prepare completed successfully." 
